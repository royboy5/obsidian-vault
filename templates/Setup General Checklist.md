### 🗺️ New Language Setup Protocol

#### 🔍 The "Hello World" Phase
- [ ] **Install Compiler/Runtime:** (e.g., Go, Rust, Ruby)
- [ ] **Verify Install:** Run version check (e.g., `go version`)
- [ ] **Package Manager:** Identify and install the standard manager (e.g., Cargo, Gem, Pip)

#### 🏗️ Project Scaffold
- [ ] **Init Project:** Run the standard init command (e.g., `cargo new`, `npm init`)
- [ ] **Git Setup:** `git init` + Create standard `.gitignore` for this language
- [ ] **Hello World:** Print text to console and run it.

#### 🧠 Learning The Basics (Immediate To-Do)
- [ ] **How to Run:** Document the command to run the app (e.g., `npm start`, `cargo run`)
- [ ] **How to Install Libs:** Document the command to add a library
- [ ] **Entry Point:** Identify the "main" file where execution starts